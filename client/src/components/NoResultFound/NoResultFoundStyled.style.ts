import { createStyles, makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() =>
  createStyles({
    emptyPlaceholder: {
      margin: "0 auto",
    },
    emptyImg: {
      width: "300px",
    },
  })
);
